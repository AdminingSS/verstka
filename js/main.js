$(document).ready(function () {

    (function () {

        const $content = $('.inside-zone__table');
        const options = {
            setHeight: 200
        };

        $content.mCustomScrollbar(options);

    })();
});